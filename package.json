{
  "name": "kr-groups",
  "version": "1.0.0",
  "description": "KR Groups - AI with human friendly interface",
  "homepage": "https://peddireddykonda17-pixel.github.io/kr",
  "scripts": {
    "dev": "next dev",
    "build": "next build && next export",
    "start": "next start",
    "deploy": "npm run build && git add out && git commit -m 'Deploy' && git push"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "next": "^14.0.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.0"
  }
}
